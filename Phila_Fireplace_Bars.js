<button id="button-previous">Previous</button>


var setSlideOne = function () {
$('heading').text("Slide 1!!!");
$('#text').text("This is where the text goes.");
$('button-next').click(setSlideTwo);




Alternative B:


var loadState = function(stateNum) {
	_.each(markers, fucntion(m) {map.removeLayer(m); });


var nextState = function () {
  if (state === 1) {
state = 2;
  } else if (state === 2) {
  state = 1;
  }
loadState
}

$(*'#next').click(nestState);

$(document).ready(function(){
loadState(1)

}
)
